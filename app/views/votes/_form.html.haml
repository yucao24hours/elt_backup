%div
  -# TODO 'target' の部分はクラスメソッドとか使って文字列展開したい(LT, SAKE, FOOD)
  Select the target.

= form_for(@vote) do |f|
  %ul
    -# @targets.each do |target|
      %li
        = f.radio_button 
        = target.title
        = target.attendee.name

  %div
    = f.label 'Comment(option)'
  %div  
    = f.text_area :comment
  = f.submit button_label 
